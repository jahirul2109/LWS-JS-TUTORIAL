let number = 0 ;
let name = "";  // Initialization above the loop 

while (number < 10) {  // Only condition involv in parameter
    name += " jahirul " + number;
    console.log(`Name: ${name} \n Roll : ${number}`); // Loop body 
    console.log(number);
    console.log(name);
    console.log(number);
    number++;   // Increment & decrement define in last step under the loop 
    
}
 console.log(name);
 console.log(number);
 
 console.log(`Name: ${name} \n Roll : ${number}`); 


// Do While Loop 

do {
    name += " jahirul " + number;
    console.log(`Name: ${name} \n Roll : ${number}`); // Loop body 
    console.log(number);
    console.log(name);
    console.log(number);
    number++;   // Increment & decrement define in last step under the loop 
    
} while (number > 10); // Condition Check

 